package com.udacity.gradle.builditbigger;

import android.view.View;

public enum  AutoLoadingAdView {
    ;
    public static void loadAdIfNotPaid(View root) {
        //We are in the paid productFlavor, so no ad to show here.
    }
}
